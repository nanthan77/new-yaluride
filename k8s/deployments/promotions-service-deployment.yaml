apiVersion: apps/v1
kind: Deployment
metadata:
  name: promotions-service-deployment
  namespace: yaluride-production
spec:
  replicas: 2
  selector:
    matchLabels:
      app: promotions-service
  template:
    metadata:
      labels:
        app: promotions-service
    spec:
      containers:
      - name: promotions-service
        image: asia-south1-docker.pkg.dev/yalu-ride/yaluride-repo/promotions-service:latest
        ports:
        - containerPort: 3008
        env:
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: supabase-secrets
              key: database-url
        - name: SUPABASE_URL
          value: "https://ehjuseflehfmvskkgham.supabase.co"
        - name: SUPABASE_SERVICE_ROLE_KEY
          valueFrom:
            secretKeyRef:
              name: supabase-secrets
              key: service-role-key
        - name: NODE_ENV
          value: "production"
        - name: JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: app-secrets
              key: jwt-secret
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3008
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3008
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: promotions-service
  namespace: yaluride-production
spec:
  selector:
    app: promotions-service
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3008
  type: ClusterIP
