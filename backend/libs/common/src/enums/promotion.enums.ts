export enum DiscountType {
  PERCENTAGE = 'percentage',
  FIXED_AMOUNT = 'fixed_amount',
}

export enum UserVoucherStatus {
  AVAILABLE = 'available',
  ACTIVE = 'active',
  USED = 'used',
  REDEEMED = 'redeemed',
  EXPIRED = 'expired',
}
